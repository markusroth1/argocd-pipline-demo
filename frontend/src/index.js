import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Optional, falls du globales CSS nutzen möchtest
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
